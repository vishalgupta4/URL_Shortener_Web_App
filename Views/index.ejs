<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <script defer src="https://use.fontawesome.com/releases/v5.7.2/js/all.js"
        integrity="sha384-0pzryjIRos8mFBWMzSSZApWtPl/5++eIfzYmTgBBmXYdhvxPc+XcFEk+zJwDgWbP"
        crossorigin="anonymous"></script>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f4f8;
            animation: backgroundColorChange 5s infinite;
        }

        @keyframes backgroundColorChange {
            0% {
                background-color: #f0f4f8;
            }
            50% {
                background-color: #f9f2ff;
            }
            100% {
                background-color: #f0f4f8;
            }
        }

        nav.navbar {
            background: linear-gradient(90deg, #3d6ed8, #554f91);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease-in-out;
        }

        nav.navbar:hover {
            transform: scale(1.05);
        }

        .navbar-brand {
            font-size: 2rem;
            font-weight: bold;
            color: #ffffff;
            transition: color 0.3s ease;
        }

        .navbar-brand:hover {
            color: #ffd700;
        }

        .container {
            max-width: 800px;
            margin-top: 50px;
            transition: all 0.3s ease-in-out;
        }

        .container:hover {
            transform: scale(1.02);
        }

        .jumbotron {
            padding: 2rem;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
            border-left: 5px solid #00c6ff;
            transition: border-color 0.5s ease;
        }

        .jumbotron:hover {
            border-left: 5px solid #50983a;
        }

        .form-control {
            height: 50px;
            font-size: 1rem;
            padding: 12px 15px;
            border-radius: 6px;
            border: 2px solid #00c6ff;
            transition: border-color 0.5s ease, box-shadow 0.3s ease;
        }

        .form-control:focus {
            border-color: #ff5e62;
            box-shadow: 0 4px 10px rgba(255, 94, 98, 0.5);
        }

        .btn-outline-success {
            width: 100%;
            font-size: 1.2rem;
            padding: 12px;
            border-radius: 6px;
            background-color: #00c6ff;
            color: #fff;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .btn-outline-success:hover {
            background-color: #ff5e62;
            transform: translateY(-3px);
        }

        .result {
            border: 1px solid rgb(203, 201, 201);
            border-radius: 6px;
            height: 36px;
            background: #f0f4f8;
            font-size: 16px;
            padding: 8px;
            text-align: center;
            margin-top: 20px;
            font-weight: 600;
            color: #007bff;
            transition: background-color 0.3s ease;
        }

        .result:hover {
            background-color: #d9f0ff;
        }

        .btn-outline-dark {
            font-size: 1rem;
            padding: 8px 15px;
            border-radius: 6px;
            margin-top: 10px;
            display: inline-block;
            background-color: #007bff;
            color: white;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        .btn-outline-dark:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="http://localhost:8009">
                URL Shortener
            </a>
            <a href="http://localhost:8009/analytics">
                <img width="50px" src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcScI6U8fbO6W-Eq2y6XfHqoHitc599LXky8-A&s" alt="anal">
            </a>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="container pt-4">
            <div class="jumbotron">
                <form method="POST" action="/url">
                    <h5> Enter Your URL </h5>
                    <input class="form-control" id="url" name="url" type="text" placeholder="Enter a valid URL..."
                        aria-label="URL" aria-describedby="basic-addon1">
                    <button class="btn btn-outline-success mt-2" type="submit">
                        Generate short URL</button>
                </form>
            </div>
            <% if (locals.id) { %>
                <div class="jumbotron">
                    <p id="generatedUrl" class="result"> http://localhost:8009/url/<%=id%>
                    </p>
                    <button class="btn btn-outline-dark" type="button" onclick="copyUrl()">
                        Copy</button>
                </div>
                <% } %>
        </div>
    </div>

    <script>
        function copyUrl() {
            const url = document.getElementById("generatedUrl").textContent
            navigator.clipboard.writeText(url)
            alert("URL copied successfully !")
        }
    </script>
</body>

</html>
